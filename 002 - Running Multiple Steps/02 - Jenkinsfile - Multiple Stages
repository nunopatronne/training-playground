pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('build'){
		stops{
			sh 'date > currDate.txt'
		}
	}
	stage('test'){
		sh 'grep "2019" currDate.txt
	}
    }
}
